interface Props {}

const AllWeeksTabContent = (props: Props) => {
  return (
    <section className="_content-load-animation w-full flex-col items-center justify-between gap-4">
      All Weeks Tab Content
    </section>
  );
};

export default AllWeeksTabContent;
