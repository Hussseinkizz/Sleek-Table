interface Props {}

const LastWeekTabContent = (props: Props) => {
  return (
    <section className="_content-load-animation w-full flex-col items-center justify-between gap-4">
      Last Week Tab Content
    </section>
  );
};

export default LastWeekTabContent;
